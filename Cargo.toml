[package]
name = "molten"
version = "0.1.0"
authors = ["transistor <trans@jabberwocky.ca>"]

[dependencies]
nom = { version = "^4.2", features = ["verbose-errors"] }
nom_locate = { version = "^0.3", features = ["verbose-errors"] }
clap = "^2"
llvm-sys = "^110"
regex = "^1.1.0"
rand = "^0.5"

